DROP DATABASE IF EXISTS movies_db;
CREATE DATABASE movies_db;

USE movies_db;

CREATE TABLE movies_db;(
  id INT ,
  movie_name VARCHAR(30) NOT NULL,
  primary id

CREATE TABLE reviews_db;(
  id INT ,
  movie_id VARCHAR(30) NOT NULL,
  review_details TEXT VARCHAR(30) TEXT,
  FOREIGN KEY (reviews_db)
  REFRENCES review(db)
  ON DELETE SET NULL